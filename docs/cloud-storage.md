# Cloud Storage

Back up to S3, GCS, or Azure Blob by choosing cloud storage during `ledger init` or in your profile YAML.

```bash
ledger init
# Storage? AWS S3
# Bucket? my-backups
```

Cloud credentials use standard provider environment variables — see [configuration.md](configuration.md).
